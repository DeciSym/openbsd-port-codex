Disable code-mode feature exposure in codex-tools on OpenBSD.

Index: codex-rs/tools/src/tool_config.rs
--- codex-rs/tools/src/tool_config.rs.orig
+++ codex-rs/tools/src/tool_config.rs
@@ -138,7 +138,7 @@ impl ToolsConfig {
             windows_sandbox_level,
         } = params;
         let include_apply_patch_tool = features.enabled(Feature::ApplyPatchFreeform);
-        let include_code_mode = features.enabled(Feature::CodeMode);
+        let include_code_mode = !cfg!(target_os = "openbsd") && features.enabled(Feature::CodeMode);
         let include_code_mode_only = include_code_mode && features.enabled(Feature::CodeModeOnly);
         let include_js_repl = features.enabled(Feature::JsRepl);
         let include_js_repl_tools_only =
